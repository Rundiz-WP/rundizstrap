Use this command:
`wp i18n make-pot . --exclude="assets/vendor,blocks-src,languages,node_modules,node_tasks,*.css,*.svg,*.eot,*.woff,*.woff2,*.ttf,*.html,*.txt,*.pot"`
And check the result.

Convert to JSON to make block i18n supported.
* No need, because there is no block in the theme. They have moved to plugin now.
* Translate text and save into .po (+.mo)
* Run this command:
    `wp i18n make-json languages/bootstrap-basic-fse-th.po --no-purge --pretty-print`
