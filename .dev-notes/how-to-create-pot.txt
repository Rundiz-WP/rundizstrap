Use this command:
`wp i18n make-pot . --exclude="assets/vendor,languages,node_modules,node_tasks,*.css,*.svg,*.eot,*.woff,*.woff2,*.ttf,*.pot"`
Or via command `npm run langPot`
And check the result.

Convert to JSON to make block i18n supported.
* !!--Currently is no need, because there is no block in the theme. They have moved to plugin now.--!!
